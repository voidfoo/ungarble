# Browser extension to fix garbled text (mojibake)

This extension provides candidates to replace garbled CJK text (Mojibake) in editable input/textarea. See all fixes and pick the one looks right to you! It can also show possible fixes for selected text even it is not editable.
